class Mapa(object):
	def __init__(self, altura, ancho, fichas, robot):
		self.altura = 0
		self.ancho = 0
		self.fichas = 0
		self.robot = robot

	def ubicar_robot(self):
		for altura in range(len(mapalista))
			for ancho in range(len(mapalista[ancho]))
				if mapa_lista[ancho][altura] == "▲"

	def imprimir_mapa(self):
		