import mapa
import moneda
import robots
import time
import uso_utilidades

mapa_eligido = input("Ingrese el mapa por favor: ")
uso_de_mapa = cargar_mapa(mapa_eligido)

instrucciones_elegidas = input("Ingrese las instrucciones elegidas por favor:")
uso_de_instrucciones = cargar_instrucciones(uso_de_instrucciones)

for y in range():
	for x in range():