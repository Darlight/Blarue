import Ruplay
class Mapa(object):
	def __init__(self, altura, ancho, fichas, robot):
		self.altura = altura
		self.ancho = ancho
		self.fichas = fichas
		self.robot = robot

	def crear_mapa(self):
		for self.altura in range(len(lineas))
		